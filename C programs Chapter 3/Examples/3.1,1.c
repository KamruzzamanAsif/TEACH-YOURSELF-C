#include<stdio.h>
#include<conio.h>

int main(){
    char ch;

    printf("Enter a character:");
    ch=getche(); //code used to scan character
    printf("\n Its ASCII code is %d",ch);

    return 0;

    }
